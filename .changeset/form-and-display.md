---
"@abek/awesome-ui": minor
---

Add Card, Badge, Spinner, Checkbox and Switch, plus a shared `useField` hook for label/description/error wiring and a `renderAsChild` helper for `asChild` components.
